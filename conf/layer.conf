BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "ronga-udoo"
BBFILE_PATTERN_jma-udoo := "^${LAYERDIR}/"
BBFILE_PRIORITY_jma-udoo = "6"

